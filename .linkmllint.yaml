# Use the recommended rule set except for the standard_naming rule
extends: recommended

rules:
  standard_naming:
    level: disabled
    # permissible_values_upper_case: true
    # class_pattern: snake_case
  canonical_prefixes:
    # Suppress warnings for https-prefixes
    # related: https://github.com/linkml/linkml/issues/2492
    level: disabled
